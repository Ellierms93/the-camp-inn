const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});
const form=document.querySelector('#searchForm'),msg=document.querySelector('#searchMessage');form.addEventListener('submit',e=>{e.preventDefault();const a=document.querySelector('#arrival').value,d=document.querySelector('#departure').value;if(!a||!d)return;if(new Date(d)<=new Date(a)){msg.textContent='Departure needs to be after arrival.';return}msg.textContent='Prototype availability: all four stays are showing below. Live inventory will connect here next.';document.querySelector('#stay').scrollIntoView({behavior:'smooth'})});
const modal=document.querySelector('#modal'),title=document.querySelector('#modalTitle'),summary=document.querySelector('#summary');document.querySelectorAll('[data-pitch]').forEach(b=>b.addEventListener('click',()=>{title.textContent=b.dataset.pitch;const a=document.querySelector('#arrival').value||'Choose an arrival date';const d=document.querySelector('#departure').value||'Choose a departure date';summary.innerHTML=`<strong>${b.dataset.pitch}</strong><br>${a} → ${d}<br>${document.querySelector('#guests').value}`;modal.hidden=false}));document.querySelector('.close').onclick=()=>modal.hidden=true;modal.addEventListener('click',e=>{if(e.target===modal)modal.hidden=true});document.querySelectorAll('.experience').forEach(b=>b.addEventListener('click',()=>b.classList.toggle('selected')));
